apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cannabis-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: cannabis-test.dbca.wa.gov.au
      http:
        paths:
          - path: /admin
            pathType: Prefix
            backend:
              service:
                name: cannabis-clusterip-test
                port:
                  number: 8000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: cannabis-clusterip-test
                port:
                  number: 8000
          - path: /files
            pathType: Prefix
            backend:
              service:
                name: cannabis-clusterip-test
                port:
                  number: 8000
          - path: /static
            pathType: Prefix
            backend:
              service:
                name: cannabis-clusterip-test
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cannabis-clusterip-test
                port:
                  number: 3000
